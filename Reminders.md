# Precompilation
- Update version number in MO2 separator
- Set executable to the game's executable
- Set active profile to `Waters of Life`
- Make sure any new mods that are disabled by default are set to `Always Enabled` in compiler settings
- Delete any uninstalled downloads
